require 'spec_helper'

describe SimpleIterator do
  it 'has a version number' do
    expect(SimpleIterator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
